[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "beike-workers"
version = "0.1.0"
description = "通用后台任务消费者（预留，未实现）"
requires-python = ">=3.11"
dependencies = []
