# 本地开发编排骨架（占位，非生产配置）
# 当前仓库仅包含服务结构，本文件仅声明服务组成，供后续完善。
services:
  gateway:
    build: ./gateway
    # TODO: ports / env_file / depends_on / 服务发现配置

  property-verification:
    build: ./services/property_verification
    # TODO: 挂载 ./services/property_verification/storage、配置环境变量

  workers:
    build: ./workers
    # TODO: 接入任务队列后启用

  # TODO: 按需补充 postgres / redis 等基础组件（当前不启用、不创建任何数据表）
