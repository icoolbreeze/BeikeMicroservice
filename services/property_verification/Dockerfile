# property_verification 服务镜像（骨架，当前不构建、不运行）
FROM python:3.11-slim

WORKDIR /app

# TODO: 安装依赖（pyproject.toml 声明后）
# TODO: 拷贝应用代码
# TODO: 配置非 root 运行用户与入口命令
# 入口示例（待应用工厂实现后启用）：
# CMD ["uvicorn", "app.main:create_app", "--factory", "--host", "0.0.0.0", "--port", "8000"]
