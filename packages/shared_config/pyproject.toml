[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "shared-config"
version = "0.1.0"
description = "公共配置加载：环境枚举与加载入口约定"
requires-python = ">=3.11"
dependencies = []
